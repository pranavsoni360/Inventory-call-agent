port: 7880
bind_addresses:
  - ""

rtc:
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true
  
keys:
  # API key/secret pairs - will be loaded from environment
  
room:
  auto_create: true
  empty_timeout: 300
  max_participants: 10

webhook:
  api_key: ${LIVEKIT_API_KEY}
  
logging:
  level: info